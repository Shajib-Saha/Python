In the QR_Code_Generator code there is a module in Python, which is imported and from which python create QR code. 
In the Create_QR_Code_Generator ther is border and sizing, border, and color of the QR code.
